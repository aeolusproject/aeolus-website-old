---
filter: haml
---
#subnavigation
  #submenu.container
    %ul.l1
      - project_pages.each do |link|
        %li
          - if !@page.subarea.nil?
            - state = (link[:key] == @page.subarea) ? 'active' : 'inactive'
          -else
            - state = (link[:href] == current_page) ? 'active' : 'inactive'
          %a{:class => state, :href => link[:href]}
            = link[:menu]
